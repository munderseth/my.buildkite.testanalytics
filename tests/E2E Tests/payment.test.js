describe('E2E Payment', () => {
    it ("Setup credit card", () => {
        expect(true).toBe(true);
    });
    it ("Change banking system", () => {
        expect(1).toEqual(1);
    });
    it ("Connect to stripe.com processing", () => {
        expect(1).toEqual(1);
    });
    it ("Apply billing usage based", () => {
        expect(1).toEqual(1);
    });
    it ("Send out notification of payment", () => {
        expect(1).toEqual(1);
    });
    it ("Invoice settings", () => {
        expect(1).toEqual(1);
    });
    it ("Email owner of acount", () => {
        expect(1).toEqual(1);
    });
    it ("Notify Owner of system change", () => {
        expect(1).toEqual(1);
    });
    it ("Respond to query n=1..i where i is settable", () => {
        expect(1).toEqual(1);
    });
    it ("Remove non-compliance order", () => {
        expect(1).toEqual(1);
    });
    it ("Reboot backend on error code 127", () => {
        expect(1).toEqual(1);
    });
});